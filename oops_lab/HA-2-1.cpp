#include <iostream>
using namespace std;

void find_union(int a[], int b[], int size_a, int size_b) {
    for(int i = 0; i < size_a,)
}

int main() {
    int a[] = {4,2,3,1,5,6,5};
    int b[] = {11,22,13,4,42};
    int size_a = sizeof(a)/sizeof(a[0]);
    int size_b = sizeof(b)/sizeof(b[0]);
    find_union(a, b, size_a, size_b);
    // find_intersection(a, b, size_a, size_b);
    return 0;
}
